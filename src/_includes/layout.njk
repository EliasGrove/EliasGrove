<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | Elias Grove</title> {# Adds site name to title #}
    <link rel="stylesheet" href="/css/style.css">
    {# Links to Google Fonts are in the CSS via @import #}
</head>
{# Adds a class like "page-home" or "page-contact" to the body #}
<body class="page-{{ pageClass | default('default') }}">
    <div class="container">
        <header>
            <h1><a href="/">Elias Grove</a></h1>
            <nav>
              <a href="/">Home</a>
              <a href="/blog/">Random</a>  {# <--- Changed text to Random #}
              <a href="/contact/">Contact</a>
            </nav>
        </header>

        <main>
            {# No automatic H1 title here anymore, control it in the markdown #}
            {{ content | safe }} {# Where page content goes #}
        </main>

        <footer>
            <p>&copy; {% year %} Elias Grove</p> {# Eleventy shortcode for year #}
        </footer>
    </div>

    {# Simple fade-in script - applies class for CSS #}
    <script>
      document.body.classList.add('loaded');
    </script>
</body>
</html>
